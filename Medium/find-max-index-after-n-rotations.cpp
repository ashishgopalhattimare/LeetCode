#include <bits/stdc++.h>

using namespace std;

// Given an array with n elements and another array with m 
// elements where each index has the value by which the
// array is rotated and the index of max element in the
// original array after each rotation is to be found

// Given an array of n elements print the count of pairs
// in the array with product less than a given value

int main()
{
    // Questions 1 
    // Find index of maximum element and then add that
    // index with all the rotations and mod the result
    // by the size of the array
    return 0;
}